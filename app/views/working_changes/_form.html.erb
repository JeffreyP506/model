<%= form_for(@working_change) do |f| %>
  <% if @working_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@working_change.errors.count, "error") %> prohibited this working_change from being saved:</h2>

      <ul>
      <% @working_change.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
